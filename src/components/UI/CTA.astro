---
import { Icon } from "astro-icon/components";

interface Props {
	href: string;
	title: string;
	ariaLabel: string;
	AdditionalClass?: string;
	text: string;
	icon: string;
	size: number;
}

const { href, title, ariaLabel, AdditionalClass, text, icon, size } =
	Astro.props;
---

<a
	href={href}
	title={title}
	aria-label={ariaLabel}
	class={`bg-primary text-white w-full h-16 py-2 px-6 rounded-xl text-center text-xl hover:scale-105 focus:scale-105 transition-all duration-300 flex items-center justify-center gap-4 lg:gap-2 xl:text-xl ${AdditionalClass}`}
>
	<Icon name={icon} size={size} />
	{text}
</a>
